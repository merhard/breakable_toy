<% @court_locations.each do |court,location| %>
  <table>
    <tr>
      <th>Courts</th>
    </tr>
    <tr>
      <td>
        <%= "#{location.street_address}, #{location.city}, #{location.state} #{location.zip}" %>
      </td>
    </tr>
    <tr>
      <td>
        <%= "Number of courts: #{court.number_of}" %>
      </td>
    </tr>
  </table>
<% end %>
